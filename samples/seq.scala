println("Step 1: How to initialize a Sequence of donuts")
val donuts: Seq[String] = Seq("Plain Donut", "Strawberry Donut", "Glazed Donut")
println(s"Elements of donuts = $donuts")
println(s"Head of donuts =${donuts(0)}")
println(s"Head of donuts =${donuts.tail}")
println(s"Head of donuts =${donuts.head}")
val wallet: Seq[Int] = Seq(100, 120, 240)
println(s"first element on my sequence is ${wallet.head} and the type is ${wallet.getClass()}")

val ints=IndexedSeq(1,2,3,4)
println(ints(2))
val smallInts = ints.filter(_ < 3)
println(smallInts)
println(smallInts.getClass())
